# This file documents the environment variables expected by the database workspace.
# It exists to make Prisma and PostgreSQL configuration explicit from the first iteration.
# It fits the system by giving migrations, local development and CI one shared source of truth.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/timelend
DIRECT_URL=postgresql://postgres:postgres@localhost:5432/timelend
