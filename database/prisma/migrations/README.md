<!-- This file explains why the migrations folder exists before the first generated migration. -->
<!-- It exists to reserve the path Prisma will use for versioned schema changes. -->
<!-- It fits the system by keeping migration history in a stable location from the first iteration. -->
# Prisma Migrations

Las migraciones reales se generarán en esta carpeta cuando se ejecute `pnpm --filter database prisma:migrate:dev`.
